/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["jurpdwnxmourevxihsoj.supabase.co"] //use the NEXT_PUBLIC_SUPABASE_URL from env
  }
}

module.exports = nextConfig
